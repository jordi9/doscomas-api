ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [com.jordi9.doscomas.AppKt.server]
  }
}

greeting {
  message = "World"
}

database {
  url = "jdbc:sqlite:doscomas.db"
  user = "prod_user"
}

tracing {
  serviceName = "doscomas"
  serviceVersion = ${?APP_VERSION}
  otlpEndpoint = "http://localhost:4317"
  otlpEnabled = false
  logFormat = "PRETTY"
}

cors {
  allowAnyLocalhost = true
}

# Local overrides (gitignored) - create application-local.conf to override settings
include "application-local"
